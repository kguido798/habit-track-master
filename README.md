# Habit Track Master

A simple habit tracking web app built with **Flask**, **HTML**, **CSS**, and **JavaScript**.

## 🚀 Features

* Add, edit, and delete habits
* Track streaks and frequency
* Clean and simple interface
* Optional API integration (e.g., quotes or weather)
* Basic error handling

## 📂 Project Structure

```
habit-track-master/
│── app.py
│── requirements.txt
│── static/
│── templates/
│── README.md
```

## 🔌 API Usage (Optional)

If you use an API:

* Store API keys in `.env`
* Fetch data using `requests`
* Display the data on the homepage


A clean and simple project for tracking habits! If you want more improvements, just ask.
